// var h = require("./sample") // All js files are modules. ./ is used because we are taking a module from our computer locally. sample.js is used here.

// h.hello();

// console.log(h.hey);

// var uc = require('upper-case'); // Didn't work for me. Deprecated. That may be the reason.

// var ucase = uc.upperCase('helloworld');

// console.log(ucase);